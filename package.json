{
    "name": "scaffold-catalyst-deluxe",
    "version": "1.0.0",
    "description": "",
    "main": "context.js",
    "scripts": {
        "init:project": "node ./scripts/init-project.js",
        "del": "rm -rf ./serve ./export",
        "build:rollup": "rollup --config rollup.config.js",
        "build:sass": "sass ./src/contexts:./serve --no-source-map",
        "export:yaml": "node ./scripts/export-yaml.js",
        "watch:rollup": "rollup --config rollup.config.js --watch",
        "watch:sass": "sass ./src/contexts:./serve --watch",
        "watch:yaml": "nodemon --watch ./export/.build ./scripts/export-yaml.js",
        "serve": "http-server ./serve --port 8080 --cors --silent",
        "build": "npm-run-all -l -s del build:* export:yaml",
        "start": "npm-run-all -ls -s del build:* -p watch:* serve"
    },
    "type": "module",
    "prettier": {
        "tabWidth": 4,
        "singleQuote": true
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@evolv-delivery/catalyst": "^0.6.4",
        "@rollup/plugin-json": "^6.0.0",
        "@rollup/plugin-node-resolve": "^15.0.1",
        "base-64": "^1.0.0",
        "http-server": "^14.1.1",
        "js-yaml": "^4.1.0",
        "nodemon": "^2.0.20",
        "npm-run-all": "^4.1.5",
        "rollup-plugin-copy": "^3.4.0",
        "rollup-plugin-json": "^4.0.0",
        "sass": "^1.54.3"
    }
}
